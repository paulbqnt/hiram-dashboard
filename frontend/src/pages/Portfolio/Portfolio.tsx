import React from 'react';
import { Container, Title } from '@mantine/core';
const Home: React.FC = () => {
    return (
      <Container size="lg">
        <Title>Portfolio</Title>
      </Container>
    );
}

export default Home;